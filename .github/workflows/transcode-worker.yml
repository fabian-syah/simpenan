name: Simpenan Video Transcoder Worker

on:
  workflow_dispatch: # Manual trigger button in GitHub Actions
  repository_dispatch:
    types: [transcode_video]
  schedule:
    - cron: '*/5 * * * *' # Fast auto-runs every 5 minutes in the cloud

jobs:
  transcode:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install System FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install Dependencies
        run: npm ci || npm install

      - name: Write .env from ENV_FILE secret
        env:
          ENV_CONTENT: ${{ secrets.ENV_FILE }}
        run: |
          printf "%s\n" "$ENV_CONTENT" > .env

      - name: Run Transcoder Batch
        run: node --env-file=.env node_modules/tsx/dist/cli.mjs scripts/worker.ts --once
